#!/bin/bash
exec cava -p /dev/stdin <<'CAVA' | sed -u 's/;//g'
[general]
framerate=1
bars=1
[output]
method=raw
raw_target=/dev/stdout
data_format=ascii
ascii_max_range=70
channels=mono
noise_reduction=0.5
CAVA